#include <stdio.h>
int main()
{
int i,j,r;
printf("Enter no. of rows needed for stairs r:");
scanf("%d",&r);
for(i=0;i<=r;i++)
{
for(j=1;j<=i;j++)
{
if(i % 2 == 1)
 {
   printf("0");
 }
else
 {
   printf("1");
 }
}
printf("\n");
}
return 0;
}
